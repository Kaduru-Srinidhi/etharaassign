# Deployment Guide - Team Task Manager on Railway

## ⚠️ IMPORTANT: This is a Monorepo - Root Directory MUST be Set

**This repository contains TWO separate services in subdirectories:**
- `backend/` → Deploy as Backend Service
- `frontend/` → Deploy as Frontend Service

**YOU MUST CREATE TWO RAILWAY SERVICES**, each with its own root directory:
1. Backend service with root directory = `backend`
2. Frontend service with root directory = `frontend`

**If you don't set the root directory, the build will fail with "No package.json found".**

See [QUICK_RAILWAY_FIX.md](QUICK_RAILWAY_FIX.md) for the 2-minute fix if this happens.

---

## Prerequisites
- GitHub account with this repository
- Railway account (https://railway.app)
- PostgreSQL database (can use Railway's PostgreSQL service)
- **Two Railway services** (backend + frontend)

## Step 1: Deploy PostgreSQL Database on Railway

1. Go to [Railway.app](https://railway.app)
2. Click "New Project"
3. Click "Provision PostgreSQL"
4. Railway will create a PostgreSQL instance and provide connection details
5. Note the `DATABASE_URL` connection string (you'll need this)

## Step 2: Deploy Backend on Railway

### ⚠️ CRITICAL: Set Root Directory to `backend`

1. In Railway, click "New Service"
2. Select "GitHub Repo"
3. Select your fork/copy of this repository
4. **BEFORE proceeding:** Click "Settings" → Find "Root Directory" field
5. **Enter:** `backend` (exactly this, no slashes, no dots)
6. Click Save
7. Railway will detect it's a Node.js project
8. In the Variables tab, add:
   - `DATABASE_URL`: Your PostgreSQL connection string
   - `JWT_SECRET`: Generate a strong secret key (e.g., use `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)
   - `NODE_ENV`: `production`
   - `PORT`: `5000`
9. Railway auto-deploys on push to main branch
10. Once deployed, note the backend URL (e.g., `https://your-app-backend.railway.app`)
11. **Verify in logs:** Should show "Server running on port 5000"

## Step 3: Deploy Frontend on Railway

### ⚠️ CRITICAL: Set Root Directory to `frontend`

1. In Railway, click "New Service"
2. Select "GitHub Repo"
3. Select repository
4. **BEFORE proceeding:** Click "Settings" → Find "Root Directory" field
5. **Enter:** `frontend` (exactly this, no slashes, no dots)
6. Click Save
7. Railway detects Vite and auto-configures the build
8. In the Variables tab, add:
   - `VITE_API_URL`: Your backend URL from Step 2 (e.g., `https://your-app-backend.railway.app`)
9. Railway auto-deploys
10. **Verify in logs:** Should show "✓ built in X.XXs"

## Step 4: Environment Variables Setup

### Backend Variables
```
PORT=5000  (Railway auto-assigns, but explicit is good)
DATABASE_URL=postgresql://username:password@host:port/database
JWT_SECRET=your-super-secret-key-make-it-long-and-random
NODE_ENV=production
```

### Frontend Variables
```
VITE_API_URL=https://your-backend.railway.app
```

## Step 5: Verify Deployment

1. Visit your frontend URL
2. Create a new account
3. Create a project
4. Create tasks
5. Test all features

## Troubleshooting Railway Deployment

### Backend Won't Connect to Database
- Verify DATABASE_URL format
- Check PostgreSQL service is running in Railway
- Ensure JWT_SECRET is set

### Frontend Can't Connect to Backend
- Verify VITE_API_URL is set correctly
- Check backend is accessible from frontend URL
- Verify CORS is enabled in backend

### Build Fails
- Check buildpacks are correct
- Verify Node version compatibility
- Check for missing dependencies
- This repository is a monorepo, so do not deploy from the root directory.
- Create two Railway services from the same repo:
  - Backend service root directory: `backend`
  - Frontend service root directory: `frontend`
- If Railway is building from repo root, image build can fail because root has no `package.json`.
- Frontend service should run a web process (`npm run start`), not only `npm run build`.

## Monitoring on Railway

1. Go to your project in Railway
2. Click on service to see logs
3. Check Variables tab for environment settings
4. Use Railway CLI: `railway logs`

## Scaling & Performance

- Railway auto-scales based on demand
- Monitor resource usage in Railway dashboard
- Consider upgrading plan for production use

## Troubleshooting

### Cold Start Issues
- Ra❌ Build Failed: "No package.json found"
**This means root directory is not set.**
- Solution: See [QUICK_RAILWAY_FIX.md](QUICK_RAILWAY_FIX.md)
- Go to service Settings, set Root Directory to `backend` or `frontend`

### Cold Start Issues
- Railway may have slight delay on first request
- This is normal for free tier

### Database Connection Pool
- Adjust connection pool in `backend/src/db/pool.js` if needed
- Railway PostgreSQL has connection limits

### Static Files Not Loading
- Ensure frontend is properly built
- Check if `dist` folder is created
- Verify build command in Railway
- Verify Root Directory is set to `frontend`
## Security Best Practices

1. **JWT_SECRET**: Use a strong, random key (32+ characters)
2. **CORS**: Already configured for development, adjust for production
3. **Database**: Use strong passwords for PostgreSQL
4. **HTTPS**: Railway provides HTTPS by default
5. **Rate Limiting**: Consider adding rate limiting middleware

## Continuous Deployment

- Every push to `main` branch auto-deploys
- Create feature branches to test before merge
- Use pull requests for code review

## Rollback

- Railway keeps deployment history
- Click "Revert" on previous deployment if needed
- Database changes may need manual rollback

## Custom Domain (Optional)

1. In Railway project settings
2. Add custom domain
3. Update DNS records as instructed
4. Update frontend API URL if needed

---

For more Railway documentation: https://docs.railway.app/
